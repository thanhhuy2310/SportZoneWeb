# SportZoneWeb

Spring Boot 3.3.5 Maven project.

## Requirements

- Java 17
- SQL Server database configured in `src/main/resources/application.properties`

## Run

Open the project root in IntelliJ IDEA, then run `SportZoneApplication` or execute `mvnw.cmd spring-boot:run` on Windows.
